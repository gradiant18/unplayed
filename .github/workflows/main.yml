name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Enter version number (e.g., v1.0.0)"
        required: true

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            platform_name: linux
            exec_ext: ""
            nuitka_flags: "--enable-plugin=pyqt6"
          - os: windows-latest
            platform_name: windows
            exec_ext: ".exe"
            nuitka_flags: "--enable-plugin=pyqt6 --windows-console-mode=disable --windows-icon-from-ico=assets/icon.ico"

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install "nuitka[onefile]" ordered-set

      - name: Build with Nuitka
        shell: bash
        run: |
          python -m nuitka --standalone \
                           --onefile \
                           --remove-output \
                           --assume-yes-for-downloads \
                           ${{ matrix.nuitka_flags }} \
                           --output-filename=unplayed \
                           src/main.py

      - name: Prepare Folder and Archive
        shell: bash
        run: |
          mkdir temp_out
          mv unplayed${{ matrix.exec_ext }} temp_out/
          mv temp_out unplayed

          if [[ "${{ matrix.platform_name }}" == "windows" ]]; then
            7z a unplayed-${{ matrix.platform_name }}-${{ github.event.inputs.version }}.zip unplayed/
          else
            zip -r unplayed-${{ matrix.platform_name }}-${{ github.event.inputs.version }}.zip unplayed/
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: unplayed-${{ matrix.platform_name }}
          path: unplayed-${{ matrix.platform_name }}-${{ github.event.inputs.version }}.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: Release ${{ github.event.inputs.version }}
          draft: true
          prerelease: false
          files: |
            unplayed-linux-${{ github.event.inputs.version }}.zip
            unplayed-windows-${{ github.event.inputs.version }}.zip
